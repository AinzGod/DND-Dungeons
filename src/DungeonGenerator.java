public class DungeonGenerator {
    public static Dungeon generateDungeon(int rows, int cols) {
        Dungeon dungeon = new Dungeon(rows, cols);
        // Aquí implementa la lógica para generar la mazmorra y colocar los elementos.
        // Puedes usar los métodos de Dungeon para establecer el contenido de cada celda.
        return dungeon;
    }
}
